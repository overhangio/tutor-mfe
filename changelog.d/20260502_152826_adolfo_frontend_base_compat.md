- [Feature] Render `env.config.compat.jsx` and wire it into the frontend-base site as a shim App via [@openedx/frontend-base-compat](https://github.com/openedx/frontend-base-compat), so legacy `PLUGIN_SLOTS` contributions and `@edx/frontend-platform` imports keep working without per-plugin ports. Opt-in per contribution via the new `FRONTEND_COMPAT_SLOTS` filter, or bulk-opt every contribution from a given Tutor plugin via `FRONTEND_COMPAT_PLUGINS`; vanilla sites and sites whose plugins haven't opted in are unaffected. (by @arbrandes)
